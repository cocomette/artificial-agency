# Orchestration Outputs

- `GameRunResult` for each game.
- Completed `m_states` rows with learner traces and snapshots.
- `run_metadata` rows for startup/runtime provenance.
- Debug events for run start/stop, frame decisions, environment steps, and
  persistence.
