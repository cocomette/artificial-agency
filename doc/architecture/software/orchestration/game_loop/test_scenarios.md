# Game Loop Test Scenarios

Automated tests cover:

- committed `agent:` configs load;
- fake-backbone learner loop execution;
- animation frame unrolling and keyframe retention;
- `ACTION6` coordinate validation;
- learner trace persistence;
- replay budgets and planner selection.
