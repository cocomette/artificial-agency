# Game Loop Test Scenarios

Current regression scenarios cover:

- frame-bundle unrolling and synthetic `NONE` animation decisions
- action history rendering and suppression hints
- change-summary structured output and fallback behavior
- game-memory prompt, repair, persistence, and prompt integration
- updater agent/general prompt behavior and output caps
- Kaggle parallel exit semantics
- runtime config rejection of removed world/goal keys
