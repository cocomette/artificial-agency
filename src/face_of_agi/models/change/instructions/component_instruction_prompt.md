## Component Guidance

`Frame components` is deterministic evidence from the attached frames.
Rows group same-color, same-shape 4-connected regions. `nb` is the count, and
`box` is normalized 0..1000 with x right and y down. Use components for
location and size; attached images remain the source of truth.
